# Terraform files
.terraform/
.terraform.lock.hcl

# Terraform state files (VERY sensitive)
terraform.tfstate
terraform.tfstate.*
*.tfstate
*.tfstate.*

# Variable files (may contain secrets)
*.tfvars
*.tfvars.json

# Crash logs
crash.log

# Sensitive override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI config files
.terraformrc
terraform.rc

# OS / editor junk
.DS_Store
Thumbs.db